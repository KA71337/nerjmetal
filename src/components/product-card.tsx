"use client";
import Link from "next/link";import { Heart,Plus } from "lucide-react";import type { Product } from "@/types";import { ProductVisual } from "./product-visual";import { useStore } from "./app-providers";
export function ProductCard({product}:{product:Product}){const {addToCart,toggleFavorite,favorites}=useStore();const liked=favorites.includes(product.id);return <article className="group panel"><Link href={`/catalog/${product.slug}`} className="relative block aspect-[4/3] overflow-hidden"><ProductVisual title={product.title} image={product.images[0]}/><span className="absolute left-4 top-4 bg-black/70 px-2 py-1 text-[10px] uppercase tracking-widest">{product.category||"Metal"}</span></Link><div className="p-5"><div className="flex gap-4"><Link href={`/catalog/${product.slug}`} className="flex-1"><h3 className="font-bold group-hover:text-[var(--acid)]">{product.title}</h3>{product.price&&<p className="mt-2 text-sm text-white/60">{product.price}</p>}</Link><button className="icon-target" onClick={()=>toggleFavorite(product.id)} aria-label={liked?"Seçilmişlərdən sil":"Seçilmişlərə əlavə et"} aria-pressed={liked}><Heart className={liked?"fill-[var(--acid)] text-[var(--acid)]":""}/></button></div><button onClick={()=>addToCart(product)} className="btn mt-5 w-full"><Plus size={15}/> Səbətə əlavə et</button></div></article>}
