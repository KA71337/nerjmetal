import type {Metadata} from "next";import {notFound} from "next/navigation";import {getSeedProduct,getSeedProducts} from "@/lib/products";import {ProductPageClient} from "./product-page-client";export async function generateStaticParams(){return getSeedProducts().map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,p=getSeedProduct(slug);return {title:p?.title||"Məhsul",alternates:{canonical:`/catalog/${slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=getSeedProduct(slug);return <ProductPageClient slug={slug} seed={product}/>}
