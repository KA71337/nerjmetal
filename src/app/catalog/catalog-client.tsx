"use client";import {useMemo,useState} from "react";import type {Product} from "@/types";import {ProductCard} from "@/components/product-card";import {useStore} from "@/components/app-providers";export function CatalogClient({seed}:{seed:Product[]}){const {products}=useStore();const all=products.length?products:seed;const [q,setQ]=useState("");const [category,setCategory]=useState("Hamısı");const categories=["Hamısı",...Array.from(new Set(all.map(p=>p.category).filter(Boolean) as string[]))];const shown=useMemo(()=>all.filter(p=>(category==="Hamısı"||p.category===category)&&p.title.toLowerCase().includes(q.toLowerCase())),[all,q,category]);return <><div className="my-10 flex flex-col gap-3 border-y border-white/10 py-5 md:flex-row"><label className="flex-1"><span className="sr-only">Məhsul axtar</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Məhsul axtar..." className="w-full border border-white/15 bg-transparent px-4 py-3"/></label><label><span className="sr-only">Kateqoriya</span><select value={category} onChange={e=>setCategory(e.target.value)} className="w-full border border-white/15 bg-[#0b0d0e] px-4 py-3">{categories.map(c=><option key={c}>{c}</option>)}</select></label></div>{shown.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{shown.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="panel p-12 text-center text-white/50">Hazırda kataloqda göstəriləcək məhsul yoxdur. Məhsulları admin səhifəsindən əlavə və ya JSON ilə import edə bilərsiniz.</div>}</>}
